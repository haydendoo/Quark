import stdio;

void doSomething(string p) {
    print(p)
}

int main() {
    int n = 1;
    char c = 'a';
    string s = "test";
    if(n == 1 && (c == 'a' || s == "test")) {
        doSomething("abc");
    }
    doSomething("aaa");
}
